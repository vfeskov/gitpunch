export default function styles (theme) {
  return {
    form: {
      display: 'flex',
      flexDirection: 'column'
    },
    formControl: {
      marginBottom: theme.spacing.unit * 2
    },
    or: {
      textAlign: 'center'
    }
  }
}
